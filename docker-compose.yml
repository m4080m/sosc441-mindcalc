version: '3.8'

services:
  mindcalc:
    build: .
    container_name: mindcalc-app
    ports:
      - "53318:53318"
    volumes:
      - ./results:/app/results
      - ./certs:/app/certs
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
